import React, { Component } from "react";

class Overlay extends Component {
  state = {
    visible: false,
  };

  render() {
    return (
      <div>
        <span>info</span>
      </div>
    );
  }
}

export default Overlay;
